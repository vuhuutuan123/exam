<p>
  <strong>Topic:</strong>
  <%= @topic.name %>

  <table class="table table-bordered">
  <tbody>
    <% @tests.each do |t| %>
      <tr>
        <td><%= t.id %></td>
        <td><%= link_to 'Show', admin_test_path(t) %> 
        | 
        <%= link_to 'Edit', 'edit_admin_topic_path(topic)' %> 
        | 
        
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
</p>
